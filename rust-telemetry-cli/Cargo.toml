[package]
name = "fleet-telemetry-cli"
version = "0.1.0"
edition = "2021"
description = "Globomantics Robot Fleet telemetry decoder and health checker"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
